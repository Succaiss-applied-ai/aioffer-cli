# 贡献指南

面向中国用户，文档、界面和错误提示使用中文；代码和协议标识保留英文。提交前阅读根目录 `AGENTS.md`。

## 本地开发

```bash
pnpm install --frozen-lockfile
pnpm typecheck
pnpm test
pnpm build
pnpm check:cloud
pnpm dev
```

从当前默认开发分支创建 `feat/功能名` 或 `fix/问题名`，通过 PR 审查，不直接推送主分支。提交格式示例：`fix(local): 修复任务重复确认保护`。

## 修复站点问题

先收集脱敏页面证据，再调整唯一注册驱动；不得用猜值、相似选项、切换驱动或直接改内部 DOM 值作为兜底。增加原失败测试、受影响成功路径和无关控件负例。不能删除断言或跳过旧成功场景来获得通过。

自动化测试与真实页面验收分开报告。没有实页证据的原型保持草稿，不能声称所有网站可用。不得为了测试提交真实申请，除非用户明确授权具体申请。

## 提交前自查

- 不包含真实简历、手机号、证件、验证码、Cookie、模型密钥或公司内部验收记录。
- 不引入 AI Offer 云端调用、账号、配额或遥测依赖。
- 不破坏确认、资料版本、岗位归属、租约、提交意图、停止和回执保护。
- 新厂商要补协议及错误用例，说明真实 Key 是否已测试，不能凭兼容地址宣称全部型号通过。
- 文档命令与构建产物一致，未验证内容明确标注。
